export interface NaviconProps {
    show_nav: () => void;           // A function that returns nothing
    isNav: boolean;               // A boolean for the toggle state
    // hide_menu: () => void;          // A boolean for the toggle state
}